<% flash.each do |message_type, message_content| %>

<div class="ui <%= convert_flash_key(message_type) %> message transition">
  <i class="close icon"></i>
  <div class="header">
    <%= message_content %>
  </div>
</div>

<% end %>

